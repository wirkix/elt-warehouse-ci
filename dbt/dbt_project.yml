name: nba_warehouse
version: "1.0.0"
config-version: 2

profile: nba_warehouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  nba_warehouse:
    staging:
      +materialized: view
      +schema: nba_staging
    marts:
      +materialized: table
      +schema: nba_marts
